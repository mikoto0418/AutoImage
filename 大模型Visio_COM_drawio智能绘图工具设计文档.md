# 基于大模型与 Visio COM 自动化的论文图形智能生成与编辑工具设计文档

## 一、作品方向概述

本作品拟设计并实现一款面向论文写作、课程设计、系统设计文档和项目汇报场景的 Windows 本地智能绘图工具。系统以大模型 API 为核心理解能力，支持用户通过自然语言描述、表结构文本、业务流程说明或上传图像来生成标准化图结构 JSON，再由本地绘图引擎生成 draw.io 可编辑图，或通过 Visio COM 自动化接口调用当前已打开的 Visio 空白页面进行自动绘图。工具重点支持论文常用的功能模块图、系统架构图、Chen ER 图、层级结构图、流程图、泳道图、数据流图等图形类型，并支持后续通过自然语言对已有图形进行增量修改，从而降低论文绘图和工程文档制图门槛，提高图形绘制效率、规范性和可编辑性。

## 二、项目背景与意义

在论文写作、课程设计、软件工程文档和项目申报材料中，图形表达是非常重要的组成部分。常见图形包括功能模块图、系统架构图、ER 图、流程图、泳道图、层级图、数据流图等。传统绘图方式主要依赖 Visio、draw.io、PowerPoint 等工具手动绘制，存在以下问题：

1. 绘图过程重复性高，用户需要反复拖拽图形、调整布局和连接线。
2. 非专业用户难以掌握不同图形的规范，例如 ER 图中 Chen ER 图和 Crow's Foot 图的区别。
3. 论文图形通常要求黑白、简洁、规整，但手动画图容易出现样式不统一、线条混乱、布局不美观等问题。
4. 已有图形修改成本较高，例如增加节点、调整层级、修改连线、统一样式等操作都需要人工重新编辑。
5. 传统 AI 生成图片无法满足论文图形“可编辑、可复用、可修改”的要求。

因此，本作品将大模型的自然语言理解能力与本地绘图工具的可编辑能力结合起来，构建一个“模型负责理解，程序负责绘图”的智能图形生成与编辑工具。其核心价值不在于生成静态图片，而在于生成可编辑、可修改、可导出的结构化图形文件。

## 三、作品定位

本系统定位为一款 Windows 本地智能绘图助手，主要面向论文写作、课程设计、毕业设计、软件工程文档和实验室项目文档制作等场景。系统不是单纯的网页绘图平台，也不是单纯的 AI 图片生成工具，而是一个综合性的本地绘图工具，能够同时支持 draw.io 嵌入式绘图和 Visio COM 自动化绘图。

作品核心定位可以概括为：

> 基于大模型 API 与 Windows COM 自动化的论文图形智能生成与自然语言编辑工具。

## 四、设计目标

### 4.1 总体目标

构建一个能够通过自然语言或图像输入生成论文常用图形的 Windows 本地程序，实现从“用户需求描述”到“可编辑图形文件”的自动化转换。

### 4.2 具体目标

1. 支持自然语言生成图形。
2. 支持上传图片，由多模态模型识别图中结构并转为可编辑图。
3. 支持生成 draw.io 可编辑文件。
4. 支持在程序内嵌入 draw.io 编辑页面，直接预览和编辑图形。
5. 支持调用当前已打开的 Visio 空白页，通过 COM 自动化接口进行绘图。
6. 支持自然语言修改已有图形。
7. 支持论文常用图形模板和规范化样式。
8. 支持图结构 JSON 保存、加载、版本记录和二次编辑。
9. ER 图默认采用 Chen ER 图风格，而不是 Crow's Foot 爪型图。
10. 输出图形应尽量符合论文常用的黑白、简洁、规范、可打印风格。

## 五、核心使用场景

### 5.1 文本生成图形

用户输入：

> 生成一个学生选课系统的 Chen ER 图，包括学生、课程、教师、选课记录，学生可以选多门课程，课程可以被多个学生选择，教师教授课程。

系统自动生成：

1. 图结构 JSON。
2. Chen ER 图。
3. draw.io 文件。
4. 可选：同步绘制到当前 Visio 空白页。

### 5.2 表结构生成 ER 图

用户输入数据库表结构：

```sql
student(id, name, gender, major)
course(id, name, credit)
teacher(id, name, title)
sc(student_id, course_id, grade)
```

系统自动识别实体、属性和关系，生成 Chen ER 图。

### 5.3 图片识别后重绘

用户上传一张手绘草图、老师给的示例图或截图，系统通过多模态模型分析图中的节点、文字、层级和连线关系，转成统一 JSON，再重新绘制为可编辑的 draw.io 图或 Visio 图。

### 5.4 自然语言修改已有图

用户在图形生成后继续输入：

> 把“用户管理”模块下面增加“权限分配”和“角色管理”，并把所有箭头改成普通直线。

系统不重新生成整张图，而是在当前图结构 JSON 的基础上执行增量修改，并同步更新 draw.io 或 Visio 页面。

### 5.5 论文图形规范化

用户输入：

> 把这张图改成论文风格，白底黑线，不要阴影，字号统一，线条全部改成直线。

系统自动调整图形样式，使其符合论文插图常用规范。

## 六、功能需求设计

### 6.1 图形生成模块

系统应支持以下图形类型：

| 图形类型 | 说明 | 默认风格 |
|---|---|---|
| 功能模块图 | 展示系统功能结构 | 层级矩形结构 |
| 系统架构图 | 展示前端、后端、数据库、模型服务等组成 | 分层架构 |
| Chen ER 图 | 展示实体、属性、关系 | 实体矩形、关系菱形、属性椭圆 |
| 层级结构图 | 展示组织结构、目录结构、模块结构 | 树形布局 |
| 流程图 | 展示处理流程、判断流程 | 标准流程图符号 |
| 泳道图 | 展示不同角色或部门之间的流程流转 | 横向或纵向泳道 |
| 数据流图 | 展示数据源、处理过程、数据存储和数据流向 | DFD 风格 |
| 类图简化版 | 展示类、属性、方法和关系 | UML 简化风格 |

### 6.2 draw.io 生成与嵌入模块

draw.io 相关功能包括：

1. 生成 `.drawio` 文件。
2. 生成 draw.io XML 内容。
3. 在程序内部通过 WebView2 嵌入 draw.io 编辑页面。
4. 支持将生成的 XML 加载到嵌入式 draw.io 页面中。
5. 支持用户在 draw.io 中继续手动编辑。
6. 支持从 draw.io 页面读取当前 XML，回写到系统图结构。
7. 支持导出 SVG、PNG、PDF 等格式。

draw.io 作为系统的稳定绘图方案，可以优先实现。它的优势是跨平台编辑能力强、文件结构开放、可嵌入页面、便于保存和二次修改。

### 6.3 Visio COM 自动化模块

Visio COM 模块用于调用本机已安装的 Microsoft Visio，实现自动绘图。系统不需要自己实现完整 Visio 编辑器，而是连接当前打开的 Visio 应用和空白页。

主要功能包括：

1. 检测本机是否安装 Visio。
2. 连接当前正在运行的 Visio 应用实例。
3. 获取当前 ActiveDocument 和 ActivePage。
4. 在当前空白页创建图形。
5. 自动放置节点、连接线和文字。
6. 设置图形样式，包括线条、填充、字号、对齐方式等。
7. 支持创建矩形、菱形、椭圆、圆角矩形、泳道、连接线等常见图形。
8. 支持导出 `.vsdx`、PDF、PNG 等格式。
9. 支持给每个 Shape 写入自定义属性，用于后续自然语言修改时定位图形。

Visio COM 绘图适合作为系统的高级能力，尤其适用于用户需要最终提交 Visio 源文件或在 Visio 中继续精修的场景。

### 6.4 多模态图像识别模块

多模态模块用于识别用户上传的图像。输入可以是：

1. 手绘草图。
2. 老师给的示例图。
3. 论文截图。
4. 旧版系统图。
5. 白板照片。

识别内容包括：

1. 图形类型。
2. 节点文字。
3. 节点之间的连线。
4. 层级关系。
5. 泳道角色。
6. ER 图中的实体、属性、关系和基数。
7. 图形整体布局方向。

识别结果统一转换为系统内部图结构 JSON。由于图像识别可能存在误差，系统应提供“识别结果确认与修正”界面，让用户在绘制前检查节点和关系。

### 6.5 自然语言修改模块

自然语言修改是本系统的重要亮点。系统需要维护当前图形的结构状态，而不是每次都重新生成图形。

用户可以输入：

1. 增加节点。
2. 删除节点。
3. 修改节点文字。
4. 修改关系名称。
5. 修改连线方向。
6. 调整图形层级。
7. 改变图形样式。
8. 将箭头改为直线。
9. 将 Crow's Foot 风格改为 Chen ER 风格。
10. 将图形改为论文黑白风格。

系统应将用户修改意图转成操作 JSON，例如：

```json
{
  "operation": "add_node",
  "target_parent": "user_management",
  "node": {
    "id": "permission_assignment",
    "label": "权限分配",
    "type": "module"
  }
}
```

对于复杂修改，可以生成操作序列：

```json
{
  "operations": [
    {
      "operation": "rename_node",
      "target": "user",
      "new_label": "学生用户"
    },
    {
      "operation": "add_node",
      "target_parent": "student_user",
      "node": {
        "id": "role_management",
        "label": "角色管理",
        "type": "module"
      }
    },
    {
      "operation": "update_style",
      "target": "all_edges",
      "style": {
        "arrow": "none",
        "line": "straight"
      }
    }
  ]
}
```

## 七、系统总体架构

系统总体架构采用“输入层—模型解析层—图结构中间层—绘图引擎层—导出层”的设计。

```text
用户输入
  ├─ 自然语言描述
  ├─ 数据库表结构
  ├─ 业务流程文本
  └─ 图片/截图/手绘图
        ↓
大模型解析层
  ├─ 文本模型
  ├─ 多模态模型
  └─ JSON 结构化输出约束
        ↓
图结构中间层
  ├─ Diagram JSON
  ├─ 节点管理
  ├─ 边管理
  ├─ 布局信息
  ├─ 样式信息
  └─ 历史版本
        ↓
绘图引擎层
  ├─ draw.io XML 生成器
  ├─ draw.io 嵌入式编辑器
  └─ Visio COM 自动化绘图器
        ↓
结果输出
  ├─ .drawio
  ├─ .vsdx
  ├─ .png
  ├─ .svg
  └─ .pdf
```

## 八、技术路线设计

### 8.1 推荐技术栈

| 模块 | 推荐技术 |
|---|---|
| Windows 客户端 | C# WPF / WinUI 3 |
| 浏览器嵌入 | WebView2 |
| Visio 自动化 | Microsoft.Office.Interop.Visio |
| draw.io 嵌入 | diagrams.net embed / WebView2 |
| JSON 处理 | System.Text.Json / Newtonsoft.Json |
| 本地数据库 | SQLite |
| 大模型接口 | OpenAI 兼容 API |
| 多模态识别 | 支持图像输入的大模型 API |
| 自动布局 | 自研规则布局 + 可扩展布局算法 |
| 配置管理 | 本地 JSON / SQLite |
| 文件导出 | draw.io XML、Visio Export、SVG/PNG/PDF |

### 8.2 为什么推荐 C# 客户端

本作品核心涉及 Windows 桌面程序和 Visio COM 自动化，因此 C# 更适合实现：

1. 与 Windows 环境兼容性更好。
2. 调用 Office COM 组件更稳定。
3. WPF/WinUI 适合开发桌面工具界面。
4. WebView2 可方便嵌入 draw.io 页面。
5. 便于管理本地文件、配置、日志和用户会话。

Python 也可以实现 COM 调用，但在打包、UI、稳定性和 Office 自动化方面不如 C# 方案适合作品落地。

## 九、核心模块设计

### 9.1 用户界面模块

界面建议分为以下区域：

1. 左侧输入区：自然语言输入、图片上传、表结构粘贴。
2. 中间预览区：嵌入 draw.io 或显示 Visio 状态。
3. 右侧结构区：展示当前图结构 JSON、节点列表、关系列表。
4. 底部操作区：生成、修改、导出、同步到 Visio、保存版本。
5. 设置区：API Key、模型选择、默认图形风格、导出路径等。

### 9.2 大模型解析模块

职责：

1. 接收用户输入。
2. 识别用户意图。
3. 判断目标图形类型。
4. 生成 Diagram JSON。
5. 对 JSON 进行格式校验。
6. 当 JSON 不合法时自动修复。
7. 对图形修改指令生成操作 JSON。

模型输出不应直接是 Visio 指令或 draw.io XML，而应是统一的中间结构。这样可以降低系统耦合度，提高可维护性。

### 9.3 Diagram JSON 中间结构模块

Diagram JSON 是系统核心。所有图形生成、修改、保存和导出都围绕该结构进行。

示例：

```json
{
  "diagram_id": "diagram_001",
  "diagram_type": "chen_er",
  "title": "学生选课系统 ER 图",
  "style": {
    "theme": "paper_black_white",
    "font": "SimSun",
    "font_size": 12,
    "line_color": "#000000",
    "fill_color": "#FFFFFF",
    "shadow": false
  },
  "layout": {
    "type": "auto",
    "direction": "left_to_right"
  },
  "nodes": [
    {
      "id": "student",
      "label": "学生",
      "type": "entity",
      "shape": "rectangle",
      "attributes": [
        {
          "id": "student_id",
          "label": "学号",
          "type": "primary_key"
        },
        {
          "id": "student_name",
          "label": "姓名",
          "type": "normal"
        }
      ]
    },
    {
      "id": "course",
      "label": "课程",
      "type": "entity",
      "shape": "rectangle",
      "attributes": [
        {
          "id": "course_id",
          "label": "课程号",
          "type": "primary_key"
        },
        {
          "id": "course_name",
          "label": "课程名",
          "type": "normal"
        }
      ]
    },
    {
      "id": "select",
      "label": "选修",
      "type": "relationship",
      "shape": "diamond"
    }
  ],
  "edges": [
    {
      "id": "edge_student_select",
      "from": "student",
      "to": "select",
      "label": "N",
      "line_type": "straight",
      "arrow": "none"
    },
    {
      "id": "edge_select_course",
      "from": "select",
      "to": "course",
      "label": "M",
      "line_type": "straight",
      "arrow": "none"
    }
  ]
}
```

### 9.4 draw.io 生成模块

职责：

1. 将 Diagram JSON 转换为 draw.io XML。
2. 根据图形类型选择不同模板。
3. 生成 mxCell 节点和边。
4. 计算节点位置。
5. 设置样式。
6. 保存为 `.drawio` 文件。
7. 加载到嵌入式 draw.io 编辑页面。

draw.io 生成模块是系统第一阶段的重点，因为其稳定性较高，且容易验证效果。

### 9.5 Visio COM 绘图模块

职责：

1. 连接当前已打开的 Visio。
2. 判断是否存在活动页面。
3. 将 Diagram JSON 转换为 Visio Shape。
4. 调用 Drop 方法放置形状。
5. 调用连接器 Shape 绘制连线。
6. 设置文字、样式、大小、位置。
7. 写入自定义属性，保存节点 ID。
8. 支持后续增量修改。

Visio COM 操作逻辑示例：

```text
连接 Visio Application
  ↓
获取 ActivePage
  ↓
清空或保留当前页面
  ↓
根据 JSON 创建实体/模块/流程节点
  ↓
根据 edges 创建连接线
  ↓
设置样式
  ↓
保存映射关系：JSON node id ↔ Visio shape id
```

### 9.6 布局模块

布局模块决定图形是否美观。不同图形应采用不同布局策略。

| 图形类型 | 推荐布局 |
|---|---|
| 功能模块图 | 自上而下树形布局 |
| 层级结构图 | 树形布局 |
| 系统架构图 | 分层布局 |
| Chen ER 图 | 实体环绕关系布局 |
| 流程图 | 自上而下或从左到右流程布局 |
| 泳道图 | 按角色分区布局 |
| 数据流图 | 分层数据流布局 |

MVP 阶段可以先采用规则布局，例如固定间距、按层级排列。后续再加入更复杂的自动布局算法。

### 9.7 历史版本与状态管理模块

为了支持自然语言修改，系统必须保存当前图形状态。

需要保存：

1. 当前 Diagram JSON。
2. draw.io XML。
3. Visio Shape ID 映射。
4. 用户修改历史。
5. 每次操作前后的版本快照。

这样可以支持：

1. 撤销。
2. 重做。
3. 对比版本。
4. 恢复历史图形。
5. 精确定位被修改的图形元素。

## 十、图形规范设计

### 10.1 默认论文风格

系统默认采用论文常用黑白简洁风格：

1. 白色背景。
2. 黑色线条。
3. 无阴影。
4. 无渐变。
5. 节点边框统一。
6. 字号统一。
7. 字体优先使用宋体、黑体或微软雅黑。
8. 连接线尽量采用直线或正交线。
9. 图形整体居中。
10. 保留足够边距，便于插入论文。

### 10.2 Chen ER 图规范

ER 图默认采用 Chen ER 图，而不是 Crow's Foot 爪型图。

Chen ER 图规则：

1. 实体使用矩形表示。
2. 关系使用菱形表示。
3. 属性使用椭圆表示。
4. 主键属性可使用下划线标识。
5. 实体与关系之间使用普通直线连接。
6. 基数标注使用 1、N、M、0..1 等文本标注。
7. 默认不使用箭头。
8. 默认不使用 Crow's Foot 爪型连接线。
9. 多对多关系应拆分为“实体—关系—实体”的结构。
10. 弱实体和多值属性可作为后续扩展功能。

### 10.3 功能模块图规范

1. 使用矩形表示模块。
2. 父模块在上，子模块在下。
3. 同级模块水平排列。
4. 模块名称简洁明确。
5. 线条默认无箭头。
6. 可选支持编号，例如 1.1、1.2、1.3。

### 10.4 泳道图规范

1. 泳道按角色、部门或系统划分。
2. 每个泳道内放置该角色负责的操作。
3. 跨泳道流程使用连接线表示。
4. 开始和结束节点使用圆角或椭圆。
5. 判断节点使用菱形。
6. 默认从左到右或从上到下流转。

## 十一、自然语言修改机制

自然语言修改分为三步：

### 11.1 理解修改意图

用户输入：

> 把 ER 图里的“用户”改成“学生”，再加一个“班级”实体，学生属于班级。

模型解析为：

```json
{
  "operations": [
    {
      "operation": "rename_node",
      "target_label": "用户",
      "new_label": "学生"
    },
    {
      "operation": "add_node",
      "node": {
        "id": "class",
        "label": "班级",
        "type": "entity"
      }
    },
    {
      "operation": "add_relationship",
      "relationship": {
        "id": "belong",
        "label": "属于",
        "from": "student",
        "to": "class",
        "cardinality": "N:1"
      }
    }
  ]
}
```

### 11.2 修改图结构 JSON

系统根据操作 JSON 修改当前 Diagram JSON。

### 11.3 同步到绘图结果

如果当前是 draw.io 模式，则更新 draw.io XML 并刷新嵌入页面。

如果当前是 Visio 模式，则通过 Shape ID 映射找到对应图形，执行增量修改。

## 十二、数据存储设计

建议使用 SQLite 保存本地数据。

主要数据表：

### 12.1 项目表 diagram_project

| 字段 | 说明 |
|---|---|
| id | 项目 ID |
| name | 项目名称 |
| diagram_type | 图形类型 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

### 12.2 图结构表 diagram_snapshot

| 字段 | 说明 |
|---|---|
| id | 快照 ID |
| project_id | 项目 ID |
| json_content | Diagram JSON 内容 |
| drawio_xml | draw.io XML 内容 |
| version_no | 版本号 |
| created_at | 创建时间 |

### 12.3 Visio 映射表 visio_shape_mapping

| 字段 | 说明 |
|---|---|
| id | 映射 ID |
| project_id | 项目 ID |
| node_id | JSON 节点 ID |
| shape_id | Visio Shape ID |
| shape_name | Visio Shape 名称 |

### 12.4 操作历史表 operation_history

| 字段 | 说明 |
|---|---|
| id | 操作 ID |
| project_id | 项目 ID |
| user_input | 用户自然语言输入 |
| operation_json | 操作 JSON |
| created_at | 创建时间 |

## 十三、API 设计

### 13.1 文本生成图形接口

输入：

```json
{
  "input_type": "text",
  "diagram_type": "chen_er",
  "content": "生成一个学生选课系统ER图，包括学生、课程、教师、选课记录……",
  "style": "paper_black_white"
}
```

输出：

```json
{
  "success": true,
  "diagram_json": {},
  "message": "图结构生成成功"
}
```

### 13.2 图片识别接口

输入：

```json
{
  "input_type": "image",
  "image_path": "C:/Users/demo/Desktop/example.png",
  "diagram_type_hint": "flowchart"
}
```

输出：

```json
{
  "success": true,
  "diagram_type": "flowchart",
  "diagram_json": {},
  "warnings": [
    "部分连接线方向识别不确定，建议用户确认"
  ]
}
```

### 13.3 自然语言修改接口

输入：

```json
{
  "current_diagram_json": {},
  "user_instruction": "把所有箭头改成直线，并把用户管理模块移动到左边"
}
```

输出：

```json
{
  "success": true,
  "operations": [],
  "updated_diagram_json": {}
}
```

## 十四、关键实现流程

### 14.1 文本生成 draw.io 流程

```text
用户输入文字
  ↓
调用大模型生成 Diagram JSON
  ↓
校验 JSON
  ↓
根据图类型选择布局算法
  ↓
生成 draw.io XML
  ↓
在 WebView2 中加载 draw.io
  ↓
用户预览和编辑
  ↓
保存 .drawio 文件
```

### 14.2 文本生成 Visio 图流程

```text
用户输入文字
  ↓
调用大模型生成 Diagram JSON
  ↓
校验 JSON
  ↓
检测 Visio 是否打开
  ↓
连接当前 Visio ActivePage
  ↓
按布局计算坐标
  ↓
通过 COM 创建 Shape
  ↓
绘制连接线
  ↓
写入 Shape 自定义属性
  ↓
完成绘图
```

### 14.3 自然语言修改流程

```text
用户输入修改指令
  ↓
读取当前 Diagram JSON
  ↓
大模型生成操作 JSON
  ↓
执行操作并更新 Diagram JSON
  ↓
更新 draw.io XML 或 Visio 页面
  ↓
保存历史版本
```

## 十五、MVP 版本设计

### 15.1 MVP 第一阶段

目标：完成可演示的基础闭环。

功能：

1. Windows 桌面程序基础界面。
2. 支持文本输入。
3. 支持调用大模型生成 Diagram JSON。
4. 支持功能模块图、流程图、Chen ER 图三类图。
5. 支持生成 draw.io XML。
6. 支持导出 `.drawio` 文件。
7. 支持 WebView2 嵌入 draw.io 页面预览。

### 15.2 MVP 第二阶段

目标：加入 Visio 自动绘图能力。

功能：

1. 检测当前 Visio 是否打开。
2. 获取当前 Visio 空白页。
3. 根据 Diagram JSON 自动绘制基础图形。
4. 支持矩形、菱形、椭圆、连接线。
5. 支持 Chen ER 图绘制。
6. 支持功能模块图绘制。

### 15.3 MVP 第三阶段

目标：加入自然语言修改能力。

功能：

1. 保存当前图结构 JSON。
2. 支持增加、删除、重命名节点。
3. 支持修改连线。
4. 支持修改样式。
5. 支持 draw.io 增量更新。
6. 支持 Visio Shape 定位与增量修改。

### 15.4 MVP 第四阶段

目标：加入多模态识别能力。

功能：

1. 支持上传图片。
2. 调用多模态模型识别图形结构。
3. 转换为 Diagram JSON。
4. 重新生成 draw.io 或 Visio 图。
5. 提供识别结果确认界面。

## 十六、创新点

1. 将大模型自然语言理解与 Visio COM 自动化结合，实现本地 Visio 自动绘图。
2. 支持 draw.io 嵌入式编辑，兼顾自动生成和人工精修。
3. 采用统一 Diagram JSON 中间结构，实现模型解析与绘图引擎解耦。
4. 支持自然语言增量修改已有图，而不是每次重新生成静态图片。
5. 支持多模态图像识别，将手绘图、截图或旧图重绘为可编辑图。
6. 面向论文常用图形场景，默认提供论文黑白规范样式。
7. ER 图默认采用 Chen ER 图规范，符合多数数据库课程和论文中的传统 ER 图要求。
8. 支持 draw.io 与 Visio 双输出，满足不同用户的编辑习惯和提交要求。

## 十七、可行性分析

### 17.1 技术可行性

本系统所需技术均有成熟基础：

1. 大模型 API 可以完成自然语言解析和 JSON 生成。
2. 多模态模型可以完成图片结构识别。
3. draw.io 文件本质为 XML，可通过程序生成。
4. draw.io 支持嵌入页面，可通过 WebView2 集成到 Windows 客户端。
5. Visio 支持 COM 自动化，可由本地程序控制图形绘制。
6. JSON、SQLite、WPF、WebView2 等技术生态成熟。

因此，本系统具备较高的技术可行性。

### 17.2 开发可行性

项目可按模块逐步实现，不需要一次性完成所有能力。优先实现 draw.io 生成，再实现 Visio COM，最后实现多模态识别和复杂自然语言修改。这样的开发路径风险较低，适合作为毕业设计、课程设计、比赛作品或实验室工具。

### 17.3 应用可行性

论文绘图和系统设计文档绘图是高频需求，用户群体明确，使用场景真实。系统生成的是可编辑图形，而不是静态图片，因此具有较强实用价值。

## 十八、难点与解决方案

### 18.1 大模型输出不稳定

问题：大模型可能输出格式错误或字段缺失。

解决方案：

1. 使用严格 JSON Schema。
2. 对模型输出进行自动校验。
3. 校验失败时让模型自动修复。
4. 在程序中设置默认值。
5. 对关键字段进行二次确认。

### 18.2 图形自动布局不美观

问题：节点较多时，图形可能拥挤或连线交叉。

解决方案：

1. 不同图类型采用不同布局规则。
2. MVP 阶段采用简单规则布局。
3. 后续加入树形布局、分层布局、正交布局等算法。
4. 允许用户在 draw.io 或 Visio 中手动微调。

### 18.3 Visio COM 环境依赖

问题：Visio COM 依赖 Windows 和本机已安装 Visio。

解决方案：

1. 将 Visio 绘图作为高级功能。
2. draw.io 作为默认稳定输出方案。
3. 程序启动时检测 Visio 环境。
4. 如果未检测到 Visio，则提示用户仅使用 draw.io 模式。

### 18.4 图片识别准确率不稳定

问题：多模态模型对复杂图形可能识别错误。

解决方案：

1. 提供图形类型提示。
2. 提供识别结果确认界面。
3. 支持用户用自然语言修正识别结果。
4. 不直接覆盖原图，先生成可检查 JSON。

### 18.5 自然语言修改定位困难

问题：用户说“把左边那个模块改一下”时，系统可能无法准确定位。

解决方案：

1. 每个节点保留唯一 ID。
2. 保存节点坐标和层级信息。
3. 支持按名称、位置、类型多方式定位。
4. 对模糊指令进行候选提示。
5. 尽量维护 JSON 与 Visio Shape ID 的映射关系。

## 十九、系统特色总结

本系统最大的特点是将大模型的理解能力与专业绘图软件的编辑能力结合起来。大模型不直接画图，而是生成标准化图结构；程序不理解复杂自然语言，而是根据结构化 JSON 稳定绘图。通过这种分工，系统既能利用 AI 降低绘图门槛，又能保证最终图形可编辑、可修改、可导出，适合论文、课程设计和工程文档等正式场景。

## 二十、预期成果

最终系统预期形成以下成果：

1. 一个可运行的 Windows 本地智能绘图程序。
2. 一套统一的 Diagram JSON 图结构规范。
3. 一个 draw.io 文件生成与嵌入式编辑模块。
4. 一个 Visio COM 自动化绘图模块。
5. 一个自然语言图形生成与修改模块。
6. 一个多模态图片识别重绘模块。
7. 一套论文常用图形模板库。
8. 一份系统设计文档、使用说明和演示案例。
9. 若干示例图，包括功能模块图、Chen ER 图、流程图、泳道图和系统架构图。

## 二十一、作品名称建议

可选名称：

1. 智图助手：基于大模型的论文图形智能生成工具
2. AI Diagram Studio：面向论文与工程文档的智能绘图系统
3. VisioDraw AI：基于 Visio COM 与 draw.io 的智能绘图助手
4. 图灵绘：自然语言驱动的可编辑图形生成工具
5. PaperDiagram AI：论文图形自动生成与编辑系统

推荐名称：

> 智图助手：基于大模型与 Visio COM 自动化的论文图形智能生成系统

## 二十二、简版作品方向表述

本作品拟设计并实现一款 Windows 本地智能绘图工具，面向论文写作、课程设计和系统设计文档中的常用图形绘制需求。系统通过大模型 API 理解用户的自然语言描述、表结构文本或业务流程说明，也可结合多模态模型识别用户上传的图片和手绘草图，并将识别结果统一转换为标准图结构 JSON。在此基础上，系统既可以生成 draw.io 可编辑图并嵌入页面进行预览和修改，也可以调用当前已打开的 Visio 空白页，通过 Visio COM 自动化接口自动绘制对应图形。系统默认支持功能模块图、系统架构图、Chen ER 图、层级图、流程图、泳道图等论文常用图形，并支持用户通过自然语言对已有图形进行增量修改，实现从“文字描述”到“可编辑规范图形”的智能化转换。
